```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
This corrected query uses the `>=` operator (greater than or equal to), correctly including employees with salaries of 100000.